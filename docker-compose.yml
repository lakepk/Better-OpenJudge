version: '3.8'

services:
  web:
    build: .
    ports:
      - "4399:4399"
    volumes:
      - judge_db:/app/db          # persist SQLite across restarts
    environment:
      - DATABASE_PATH=/app/db/judge.db
    restart: unless-stopped

volumes:
  judge_db:
